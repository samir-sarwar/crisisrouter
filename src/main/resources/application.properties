# 1. Database Connection Settings
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/crisis_router}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:devuser}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:devpassword}
spring.datasource.driver-class-name=org.postgresql.Driver

server.port=${PORT:8080}
server.forward-headers-strategy=framework

# 2. Hibernate/JPA Settings (The "Magic" Part)
# 'update' tells Hibernate to automatically create/modify tables based on your @Entity classes
# Hibernate 6+ automatically detects the dialect from the JDBC URL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# 3. General Settings
spring.main.allow-bean-definition-overriding=true

aws.s3.bucket-name=${S3_BUCKET_NAME}
aws.s3.region=us-east-2
aws.s3.access-key=${AWS_ACCESS_KEY}
aws.s3.secret-key=${AWS_SECRET_KEY}

# Auth0 Configuration
spring.security.oauth2.client.registration.auth0.client-id=${AUTH0_CLIENT_ID}
spring.security.oauth2.client.registration.auth0.client-secret=${AUTH0_CLIENT_SECRET}
spring.security.oauth2.client.registration.auth0.scope=openid,profile,email
spring.security.oauth2.client.registration.auth0.redirect-uri=${BACKEND_URL:http://localhost:8080}/login/oauth2/code/auth0
spring.security.oauth2.client.provider.auth0.issuer-uri=https://${AUTH0_DOMAIN}/

# Map the Auth0 sub/name to the Spring Security user
spring.security.oauth2.client.registration.auth0.client-name=Auth0
spring.security.oauth2.client.registration.auth0.client-authentication-method=client_secret_post

# Frontend URL (for CORS and OAuth redirects)
app.frontend-url=${FRONTEND_URL:http://localhost:5173}

# Debug OAuth2 issues (remove after fixing)
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.oauth2=TRACE